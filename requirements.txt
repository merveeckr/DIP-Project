# COM-4504 DIP Project — Merve Çakır
# YOLOv8s + SAHI + ByteTrack pipeline
# Python 3.10+ | GPU: NVIDIA Tesla T4 (Google Colab önerilir)

# ── Deep Learning & Object Detection ──────────────────────────────────────────
ultralytics>=8.0.200          # YOLOv8s training, inference, evaluation
torch>=2.0.0                  # PyTorch (CUDA 11.8+ önerilir)
torchvision>=0.15.0

# ── SAHI (Slicing Aided Hyper Inference) ──────────────────────────────────────
sahi>=0.11.15                 # Küçük nesne tespiti için dilim tabanlı çıkarım

# ── Multi-Object Tracking ──────────────────────────────────────────────────────
supervision>=0.18.0           # ByteTrack entegrasyonu + annotation utilities
lapx>=0.5.5                   # Hungarian algorithm (ByteTrack LAP solver, numpy 2.x uyumlu)

# ── Computer Vision ────────────────────────────────────────────────────────────
opencv-python>=4.8.0          # Video okuma/yazma, frame işleme (cv2)
Pillow>=10.0.0                # Görüntü yükleme/kaydetme

# ── Data & Augmentation ───────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
albumentations>=1.3.0         # Mosaic ve veri augmentation pipeline'ı

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Model Profiling ───────────────────────────────────────────────────────────
thop>=0.1.1                   # GFLOPs ve parametre sayımı

# ── Report Generation ─────────────────────────────────────────────────────────
python-docx>=1.1.0            # IEEE format Word belgesi üretimi
